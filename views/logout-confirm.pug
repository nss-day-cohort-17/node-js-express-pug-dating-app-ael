extends partials/layout.pug

block content
	.heading: h1 Are you sure you want to log out, #{currentUser.first_name} #{currentUser.last_name}?

	a.btn.btn-default.btn-lg(href="/home") Return home
	a.btn.btn-danger.btn-lg(href="/logout") Log out
